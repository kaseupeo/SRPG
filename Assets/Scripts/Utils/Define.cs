public class Define
{
    public enum SceneType
    {
        LobbyScene,
        GenerateCharacterScene,
        GameScene,
    }
    
    public enum ItemType
    {
        Equipment,
        Consumption,
    }
    
    public enum CreatureState
    {
        Idle,
        Move,
        Attack,
        Dead,
    }
}